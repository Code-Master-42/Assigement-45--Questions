"use strict";
let username = [];
if (username.length === 0) {
    console.log("We need find to some users!");
}
else {
    //Greet user
    //Removing all username ensures the message "we need to find some users"is printed
}
;
